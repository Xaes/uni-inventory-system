stages:
    - build
    
before_script:
    - 'dotnet restore'
    
variables:
    project: "Domain"

build_nuget:
    stage: build
    image:
        name: mcr.microsoft.com/dotnet/core/sdk:3.0.101-disco
    script:
        - 'dotnet msbuild InventorySystem.sln /t:Clean,ReBuild /p:Configuration=Release;Platform="Any CPU"'
        - 'dotnet nuget push /builds/xaesdesign/inventory-system/Domain/bin/Release/Domain.1.0.0.nupkg'
    only:
        - master
